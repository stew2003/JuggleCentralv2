module.exports = {
  // amount by which to scale up user scores (higher scale, greater precision, also more excitement)
  USER_SCORE_SCALING_FACTOR: 100
  /*	Required domain for new user account emails.
		Use a regular expression that will match any email from your domain. */

  // EMAIL_DOMAIN_RESTRICTION: /.+?@(students\.)?stab\.org/
}
